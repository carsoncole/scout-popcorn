- if @active_event && @resources.any?
  .card
    .card-body
      %h5.card-title.resources More info

      %ul.card-text
        - @resources.each do |resource|
          %li.resource
            = link_to resource.url do
              = resource.name
              %i.fa.fa-external-link