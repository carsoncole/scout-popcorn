.row
  .col-md-12
    .card
      .card-block
        .card-title 
          %h1 Online Sales
        .alert.alert-info
          All sales that are done on the Trails-End website using your Scout code will show up here.
        - if @online_sales.any?
          .table-responsive
            %table.table
              %thead
                %tr
                  - if current_scout.admin?
                    %th Scout
                    %th Event
                  %th Order date
                  %th Customer name
                  %th Description
                  %th Amount
                  - if current_scout.admin?
                    %th
                    %th

              %tbody
                - @online_sales.each do |online_sale|
                  %tr
                    - if current_scout.admin?
                      %td= online_sale.scout.name
                      %td= online_sale.event.name
                    %td= online_sale.order_date
                    %td= online_sale.customer_name
                    %td= online_sale.description
                    %td= online_sale.amount
                    - if current_scout.admin?
                      %td
                        = link_to edit_online_sale_path(online_sale) do
                          %i.fa.fa-pencil.fa-lg
                      %td
                        = link_to online_sale, :method => :delete, :data => { :confirm => 'Are you sure?' } do
                          %i.fa.fa-trash.fa-lg

        %br

        - if current_scout.admin?
          = link_to new_online_sale_path do
            %i.fa.fa-plus.fa-lg
