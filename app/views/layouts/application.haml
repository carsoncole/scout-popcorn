!!!
%html(lang='en')
  = render 'head'
  
  %body
    .hidden_print=render 'nav'
    - if controller_name == 'ledgers' && action_name == 'index'
      .container-fluid
        .hidden_print= render 'flash'
        = yield
    - else
      .container
        .hidden_print= render 'flash'
        = yield
        / = render 'debugging'