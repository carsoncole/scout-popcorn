- if target.errors.any?
  .row
    .col-md-8
      #error_explanation
        .alert.alert-danger
          %h5= "#{pluralize(target.errors.count, "error")} prohibited this take order from being saved:"
          %ul
            - target.errors.full_messages.each do |msg|
              %li.text-error= msg