.row
  .col-md-12
    .card
      .card-block
        .card-title
          %h1 Accounts

        %table.table
          %thead
            %tr
              %th Name
              %th Balance
              %th.text-xs-center
                Take
                %br
                Orders
              %th.text-xs-center
                Site
                %br
                Sales
              %th.text-xs-center
                Bank
                %br
                Depositable
              %th.text-xs-center
                Credit Card
                %br
                %i.fa.fa-credit-card
              %th.text-xs-center
                Cash
                %br
                %i.fa.fa-money
              %th
              %th
          %tbody
            - @accounts.each do |account|
              %tr
                %td= link_to account.name, ledgers_path(account_id: account.id)
                %td= number_to_currency(account.balance)
                %td.text-xs-center
                  - if account.is_take_order_eligible?
                    %i.fa.fa-check
                %td.text-xs-center
                  - if account.is_site_sale_eligible?
                    %i.fa.fa-check
                %td.text-xs-center
                  - if account.is_bank_account_depositable?
                    %i.fa.fa-check
                %td.text-xs-center
                  - if account.is_credit_card?
                    %i.fa.fa-check
                %td.text-xs-center
                  - if account.is_cash?
                    %i.fa.fa-check
                %td= account.account_type
                %td
                  = link_to edit_account_path(account) do
                    %i.fa.fa-pencil.fa-lg
                %td
                  = link_to account, :method => :delete, :data => { :confirm => 'Are you sure?' } do
                    %i.fa.fa-trash.fa-lg

        %br

        = link_to new_account_path do
          %i.fa.fa-plus.fa-lg
